[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "car-catalog"
version = "0.1.0"
description = "Laboratory project for Professional Python"
requires-python = ">=3.11"

[project.scripts]
car-catalog = "car_catalog.main:main"